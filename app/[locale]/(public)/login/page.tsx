import LoginLayout from '@/components/layouts/login-layout';
import LoginForm from '../components/login-form';

const LoginPage = () => (
	<LoginLayout className="flex items-center justify-center">
		<LoginForm />
	</LoginLayout>
);

export default LoginPage;
